# 아키텍처

레이어의 책임과 레이어 사이의 호출·처리 흐름을 다룬다. 아래 표에서 지금 하려는 일에 맞는 문서를 골라 끝까지 읽는다. 여기에 없는 주제는 `../README.md`의 분류표로 돌아간다.

## 문서

| 이럴 때 읽는다                                                                                                                                                                      | 문서                                                      |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| 서버에 있는 데이터를 브라우저가 읽거나 바꾸는 코드를 만들거나 고칠 때 — Server Component 직접 조회, Server Action, `route.ts`, 클라이언트 훅, 폼 제출 중 무엇으로 할지 정해야 할 때 | [서버-클라이언트 데이터 흐름](server-client-data-flow.md) |
| 가져온 데이터를 언제까지 재사용할지 정할 때 — 페이지 캐시·ISR·외부 API 응답 캐시·mutation 직후 갱신                                                                                 | [캐싱과 재검증](caching-and-revalidation.md)              |
| services에서 난 에러가 사용자 화면까지 가는 경로를 만들거나 바꿀 때 — 에러 분류 추가, 응답 envelope 변경, 필드 에러 노출 방식 변경                                                  | [에러 처리](error-handling.md)                            |

## 작성 예정

- 렌더 스트리밍: `loading.tsx`·`<Suspense>`로 화면을 나누는 기준 (현재 결정 사항 없음 — `loading.tsx` 2곳, `Suspense` 1곳이 규칙 없이 존재)
- 시스템 개요: `src/` 역할별 디렉터리의 분할 배경과 주요 의존 방향
