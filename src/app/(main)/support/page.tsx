import { SupportTemplate } from "@/app/(main)/support/_components/SupportTemplate";

// 정적 셸 — mock 데이터로 화면만 구현, 실제 FAQ/문의 폼(API 연동)은 별도 작업.
export default function SupportPage() {
  return <SupportTemplate />;
}
